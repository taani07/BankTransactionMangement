package CashBackReturn;

public class main {
    public static void main(String[] args) {
     //   CashBackReturn cashBackReturn = new CashBackReturn();
        double amount = 100.0; // Example amount
      //  double cashBack = cashBackReturn.calculateCashBack(amount);
        System.out.println("Cash Back for amount $" + amount + " is: $");
    }
}
